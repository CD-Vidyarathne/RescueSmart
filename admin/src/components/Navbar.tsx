import React from "react";

const Navbar: React.FC = () => {
  return (
    <nav className="bg-blue-600 p-4">
      <h2 className="text-white text-center text-xl font-semibold">
        RescueSmart | Admin Panel
      </h2>
    </nav>
  );
};

export default Navbar;
